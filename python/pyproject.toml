[project]
name = "quantify"
version = "0.1.0"
description = "Quantitative trading research platform"
requires-python = ">=3.11"

dependencies = [
    "akshare>=1.14.0",
    "pyyaml>=6.0",
    "loguru>=0.7.0",
]
